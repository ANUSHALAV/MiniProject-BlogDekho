<!doctype html>
<html>

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upload Profile</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="bg-zinc-700 text-zinc-300 font-sans h-screen">
        <main class="p-6">
            <div class="container mb-5">
                <h2 class="text-2xl">Upload Profile Image</h2>
            </div>
            <form action="/uploadProfileImage" method="post">
                <div class="mb-5">
                    <input type="file" name="profileImage" id="profileImage"
                        class="w-md p-2 border border-zinc-400 rounded" />
                </div>
                <div class="flex justify-between items-center w-md">
                    <button class="bg-blue-500 text-white px-4 py-2 rounded" type="submit">Upload Image</button>
                    <a href="/profile" class="text-white border-2 border-zinc-400 px-4 py-2 rounded">Cancel</a>
                </div>
            </form>
        </main>
    </div>
</body>

</html>