<!doctype html>
<html>

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="bg-zinc-700 text-zinc-300 font-sans h-screen">
        <header class="flex justify-between items-center px-4 py-2 bg-zinc-800">
            <h1 class="text-2xl font-bold">Blog Dekho</h1>
            <nav class="flex justify-between items-center">
                <div class="text-zinc-200 flex items-center justify-between mr-4">
                    <a href="/uploadProfile" class="h-16 w-16 rounded-full border-2 border-zinc-400 mr-2 overflow-hidden">
                        <img src="/images/uploadImages/<%=user.profileImage%>" alt="user profile image" class="h-full w-full object-contain">
                    </a>
                    <span>Hey, <%=user.fullName%>👋</span>
                </div>
                <a href="/logout" class="text-zinc-800 rounded px-3 py-2 bg-red-400">Logout</a>
            </nav>
        </header>

        <main class="p-6">
            <form action="/uploadPost" method="post">
                <div class="mb-4">
                    <textarea class="w-md p-2 border border-zinc-400 rounded" name="content" id="content" rows="4"
                        placeholder="What's on your mind ?"></textarea>
                </div>
                <button class="bg-blue-500 text-white px-4 py-2 rounded" type="submit">Create New Post</button>
            </form>

            <div class="mt-5 flex justify-center items-center wrap gap-4">
                <% if(posts){ %>

                    <% posts.forEach(post=>{%>
                        <div class="border border-zinc-400 rounded p-4 mt-6">
                            <div class="mb-4 text-blue-300">
                                <h2>@<%=post.postBy.fullName%></h2>
                            </div>
                            <p class="mb-5">
                                <%=post.content%>
                            </p>
                            <div class="mb-2">
                                <span><%=post.likes.length%></span>
                                <a href='/like/<%= post._id %>' class="text-sm text-blue-400 cursor-pointer"><%=post.likes.includes(user._id)==false? "👍" :"👎"%></a>
                            </div>
                        </div>

                        <%})%>
                                <% } else { %>
                                    <div class="text-center text-zinc-400">
                                        <p>No posts available.</p>
                                    </div>
                                    <% } %>
            </div>
        </main>
    </div>
</body>

</html>